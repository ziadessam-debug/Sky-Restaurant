# 🍽️ Sky Restaurant - نظام طلب الطعام

نظام متكامل لطلب الطعام من مطعم Sky Restaurant مع واجهة أمامية (Frontend) وخلفية (Backend).

## 📋 المميزات

### الواجهة الأمامية (Frontend)
- ✅ عرض قائمة الطعام مع الصور والأسعار
- ✅ تصفية المنتجات حسب الفئة
- ✅ البحث في المنتجات
- ✅ سلة التسوق مع إمكانية تعديل الكميات
- ✅ قائمة المفضلة (Wishlist)
- ✅ نظام الكوبونات والخصومات
- ✅ صفحة الدفع وإتمام الطلب
- ✅ أرشيف الطلبات السابقة
- ✅ تصميم متجاوب لجميع الأجهزة

### الواجهة الخلفية (Backend)
- ✅ API كامل للمنتجات والطلبات والكوبونات
- ✅ قاعدة بيانات SQLite
- ✅ التحقق من صحة الكوبونات
- ✅ إدارة الطلبات
- ✅ لوحة إحصائيات للمشرفين

## 🚀 طريقة التشغيل

### المتطلبات
- Node.js (الإصدار 16 أو أحدث)
- npm

### خطوات التشغيل

. **انتقل لمجلد الـ backend:**
```bash
cd backend
```
. **تثبيت المكتبات المطلوبة:**
```bash
npm install
```

. **تشغيل السيرفر:**
```bash
npm start
```

. **افتح المتصفح وادخل على:**
```
http://localhost:3000
```

## 📡 API Endpoints

### المنتجات (Products)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/products` | جلب جميع المنتجات |
| GET | `/api/products/:id` | جلب منتج واحد |
| GET | `/api/categories` | جلب الفئات |

### الكوبونات (Coupons)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/coupons` | جلب جميع الكوبونات |
| POST | `/api/coupons/validate` | التحقق من صلاحية كوبون |

### الطلبات (Orders)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/orders` | جلب جميع الطلبات |
| GET | `/api/orders/:orderId` | جلب طلب واحد |
| POST | `/api/orders` | إنشاء طلب جديد |
| PATCH | `/api/orders/:orderId/status` | تحديث حالة الطلب |

### إدارة (Admin)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/admin/stats` | إحصائيات عامة |
| POST | `/api/admin/products` | إضافة منتج جديد |
| PUT | `/api/admin/products/:id` | تعديل منتج |
| DELETE | `/api/admin/products/:id` | حذف منتج |
| POST | `/api/admin/coupons` | إضافة كوبون جديد |
| DELETE | `/api/admin/coupons/:code` | حذف كوبون |

## 🎫 الكوبونات المتاحة

| الكود | الخصم | الحد الأدنى للطلب |
|-------|-------|------------------|
| WELCOME10 | 10% | $0 |
| OFFER15 | 15% | $25 |
| OFFER5 | $5 | $15 |
| SUPER20 | 20% | $40 |
| QUICK10 | 10% | $20 |

## 📁 هيكل المشروع

```
project after futuers/
├── index.html          # الصفحة الرئيسية
├── style.css           # أنماط CSS
├── script.js           # كود JavaScript للواجهة
├── README.md           # ملف التوثيق
└── backend/
    ├── package.json    # إعدادات المشروع
    ├── server.js       # سيرفر Express
    └── database.sqlite # قاعدة البيانات (يتم إنشاؤها تلقائياً)
```

## 🔧 التقنيات المستخدمة

### Frontend
- HTML5
- CSS3
- JavaScript (Vanilla)
- Font Awesome

### Backend
- Node.js
- Express.js
- SQLite (better-sqlite3)
- CORS

## 📞 الدعم

لأي استفسارات أو مشاكل، يرجى التواصل معنا.

---
Made with ❤️ for Sky Restaurant


